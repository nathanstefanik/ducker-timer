# append to the existing Caddyfile on the Caddy host (192.168.1.220);
# replace the IP with the timer LXC's LAN address
timer.nathanstefanik.xyz {
    bind 192.168.1.220
    reverse_proxy 192.168.1.XXX:8000
}
